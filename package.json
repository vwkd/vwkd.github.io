{
  "scripts": {
    "dev": "rm -r build & npx @11ty/eleventy --serve",
    "prod": "rm -r docs & npx @11ty/eleventy --output=docs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2",
    "@11ty/eleventy-plugin-syntaxhighlight": "^3.0.2"
  },
  "private": true
}
